import React from 'react';
import Table from '../../../../core/form/Table';

const StudentDetailAchievements = () => (
  <Table
    rows={[]}
    columns={[
      {
        label: 'Date',
      },
      {
        label: 'Achievement',
      },
    ]}
  />
);

export default StudentDetailAchievements;
